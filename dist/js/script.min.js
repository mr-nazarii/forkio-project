const content=document.querySelector(".nav__list"),menuBtn=document.getElementById("check");let menuOpen=!1;document.addEventListener("click",e=>{let n=e.target;"check"===n.className.toLowerCase()||"nav__hamburger"===n.className.toLowerCase()||"check-span"===n.className.toLowerCase()?menuBtn.addEventListener("click",()=>{menuOpen=menuOpen?(content.style.removeProperty("display"),!1):(content.style.cssText="display: inline-block",!0)}):(content.style.removeProperty("display"),menuBtn.checked=!1,menuOpen=!1)});